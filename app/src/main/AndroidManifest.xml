<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bacchoterra.financetrackerv2">

    <application
        android:name=".application.FinanceApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinanceTrackerV2">
        <activity android:name=".view.ShowStockActivity"></activity>
        <activity android:name=".view.AddStockActivity" />
        <activity android:name=".view.StocksActivity" />
        <activity android:name=".view.MainActivity" />
        <activity
            android:name=".view.ChatActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".view.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>